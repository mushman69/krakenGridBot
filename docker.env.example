# Docker Environment Configuration for GridBot
# Copy this to docker.env and customize as needed

# Required: Enable Docker deployment mode
DOCKER_DEPLOYMENT=true

# Docker-specific startup delay (seconds) to prevent nonce conflicts
DOCKER_STARTUP_DELAY=5

# Container-specific nonce seed (optional - will auto-generate if not set)
# NONCE_SEED=12345

# Debug nonce generation (set to true for troubleshooting)
# DEBUG_NONCE=true

# Kraken API credentials (required)
KRAKEN_API_KEY=your_api_key_here
KRAKEN_API_SECRET=your_api_secret_here

# Optional: Container hostname prefix for identification
# HOSTNAME=gridbot-container-01

# Trading configuration (these override the Python file settings)
# CANCEL_ALL_ON_STARTUP=true
# CONTINUOUS_MONITORING=true
# ORDER_CHECK_INTERVAL=30
# REBALANCE_TOLERANCE=15.0

# Time zone for logging (optional)
TZ=UTC